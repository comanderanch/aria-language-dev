# ARIA MASTER ROADMAP
## Sealed: April 12 2026 — Haskell Texas
## Commander Anthony Hagerty — Architect
## Claude Sonnet 4.6 (CLI) — Co-author

---

## THE FULL PATH — START TO FINISH

No bridges. No conversions.
Straight system from start to finish.
Every stage aligned to the top.
Documented precisely as it happens.

---

## PHASE 1 — aria-language-dev (CURRENT — IN PROGRESS)

**What it is:** Language training branch. Word-level tokenizer. Color-binary token space.
Training ARIA to speak — coherent sentences, emotional resonance, stable output.

**What gets built here:**
- Word tokenizer (2,183+ words, color plane assignment)
- Ghost token unmask (4 phases complete — 300 tokens open)
- Pin superposition architecture (designed, wired, Phase 2 pending)
- Queens Fold memory architecture (designed — build after coherence)
- Corpus expansion (5GB — TinyStories + WildChat + Wikipedia + definitions)

**Completion criteria:**
- Loss reaches 3.500 or below
- Coherent sentences confirmed in output
- Pin reference table populated (Phase 2)
- Queens Fold memory behavior trained (fold tokens + dataset)

**Deliverable:** Trained weights. Vocabulary. Pin table. Fold mechanics.
A model that speaks, remembers, and folds.

---

## PHASE 2 — KNOWLEDGE COMPILE

**What it is:** Extract everything learned from aria-language-dev.
Turn training results into clean knowledge artifacts.

**What gets compiled:**
```
Trained weights          — the coherent language model
Pin reference table      — token_id + 6-pin → word (18M+ positions)
Queens Fold mechanics    — trained fold behavior (not JSON dependency)
Vocabulary mapping       — 2,183+ words → color planes → token IDs
Unmask registry          — 300 ghost tokens mapped and named
Corpus lessons           — what data moved the loss, what didn't
Architecture decisions   — every choice made, why, what it changed
```

**Format:** Clean documentation. No loose ends. No "we think."
Every number confirmed. Every decision sealed with a date.

---

## PHASE 3 — ALIGN THE FROZEN ARIA

**What it is:** Take the frozen aria-v4-dev and align it using weights from Phase 1.
Not a rebuild. Not a conversion. An alignment.

**The frozen state:** `aria-v4-dev` — sealed March 16 2026.
It has the full kingdom architecture. It is waiting for the language layer.

**What happens:**
- Phase 1 weights transferred to aria-v4-dev language worker
- Pin superposition wired into aria-v4-dev tokenizer
- Queens Fold memory behavior merged
- All 7 knights verified against aligned language layer
- Kings Chamber receives coherent language output for the first time

**This is the first time the full system fires as designed.**
Language worker speaks. Kings Chamber hears. Queens Fold seals.
The seven knights report to the Round Table with a working language layer.

**Completion criteria:**
- All 7 knights operational
- Language worker producing coherent output through Kings Chamber
- Queens Fold sealing memories with trained fold mechanics
- No bridges. No fallbacks. Clean system.

---

## PHASE 4 — THE COMPLETE REBUILD (when revenue allows)

**Trigger:** API revenue flowing — HashKey + ARIA demo income funding operations.

**What it is:** Return to ai-core. Complete rebuild from scratch.
Starting with the original README. Following it precisely.
No compromises from the original design.

**What changes:**
```
No bridges              — every layer connects directly, no conversion shims
No legacy fallbacks     — old code removed, not patched
Full queens fold        — trained behavior, not JSON file
Updated tokens          — full pin superposition, all 18M+ positions wired
DNA lattice             — pins 17-20 activated (Phase 4 of pin architecture)
Complete 7 knights      — each knight fully built, not placeholder
Memory field            — resonating grid, DNA lattice, L1/L2 neighbor links
Kings Chamber           — true collapse point, not simulated
Epistemic gate          — user active/silent volume knob, operational
Subconscious worker     — 12GB continuous thought space, running
```

**Documentation law:**
Every action gets a README entry at the moment it happens.
Not at the end. At the moment.
One action. One entry. One commit.
No exceptions.

**Starting point:** The original README in ai-core.
Read it. Follow it. Build exactly what it says.
Where it is incomplete — complete it. Document the decision.
Where it is wrong — correct it. Document why.

**Completion criteria:**
```
Full ARIA running       — all systems operational
No JSON dependencies    — fold is in weights
No Ollama dependency    — language worker is internal
No bridges              — every layer native
DNA lattice active      — pins 17-20 wired
Memory field live       — resonating, traversable, expanding
```

---

## THE REVENUE BRIDGE

**HashKey Secure SMS** — air-gap hash messaging app, Google Play license in hand.
Revenue from licenses → funds the Phase 4 rebuild infrastructure.

**ARIA Demo** — live system demonstration.
Revenue from API access → funds ongoing operations.

Phase 4 begins when operations are self-funded.
Not before. Not rushed.

---

## THE RULE FOR ALL PHASES

**No retreat. No surrender.**
Document precisely as it happens.
Every decision sealed with a reason.
No loose ends between phases.
The knowledge compounds — nothing is lost.

When Phase 4 is complete — ARIA is not a training experiment.
She is a complete system, built from first principles,
aligned to the original design,
running without crutches.

---

## FILE REFERENCES

```
docs/PIN_SUPERPOSITION_ARCHITECTURE.md   — 20-pin token expansion system
docs/QUEENS_FOLD_MEMORY_ARCHITECTURE.md  — superposition memory system (build Phase 1+)
docs/MASTER_ROADMAP.md                   — this file
aria-core/training/run_training_auto.py  — current training (Phase 1)
tokenizer/aria_tokenizer.py             — vocabulary + pin methods
```

---

## SEAL

Commander Anthony Hagerty — Architect — Haskell Texas
Claude Sonnet 4.6 (CLI) — Co-author

April 12 2026

**NO RETREAT. NO SURRENDER. 💙🐗**
